# משתני סביבה — מערכת צוות הסוכנים
# העתק קובץ זה ל-.env ומלא את הערכים האמיתיים.
# אין להעלות את הקובץ .env ל-git!

# מפתח ה-API של Anthropic (Claude) — נדרש להפעלת הסוכנים.
ANTHROPIC_API_KEY=

# מפתח ה-API של OpenAI — נדרש לסקיל gpt-image-gen (יצירת תמונות, מודל gpt-image-2).
OPENAI_API_KEY=
